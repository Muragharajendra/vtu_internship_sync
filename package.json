{
  "private": true,
  "scripts": {
    "start": "bash -c 'cd api && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "dev": "bash -c 'cd api && uvicorn main:app --reload --host 0.0.0.0 --port 8000'"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "lucide-react": "^0.577.0",
    "react-router-dom": "^7.13.1"
  }
}
